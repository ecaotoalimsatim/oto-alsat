{
  "extends": "next/core-web-vitals",
  "rules": {
    "@typescript-eslint/no-explicit-any": "off",
    "@typescript-eslint/no-unused-vars": "off"
  }
}
```

Yani dosyada sadece bu 8 satır kalmalı, başka hiçbir şey olmamalı.

Düzelttikten sonra **Ctrl + S** ile kaydet.

Sonra terminale sırayla yaz:
```
git add .
```
```
git commit -m "eslint fix"
```
```
git push